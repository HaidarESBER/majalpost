---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [shared/types/index.ts, shared/types/api.ts, frontend/src/lib/api.ts, frontend/package.json, backend/src/types/index.ts, package.json]
autonomous: true
---

<objective>
Create shared types package and API client setup for frontend-backend communication.

Purpose: Establish type-safe communication between frontend and backend with shared TypeScript types.
Output: Shared types directory, configured API client in frontend, monorepo-style package.json scripts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# These summaries will exist after Wave 1 completes:
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared types package</name>
  <files>shared/types/index.ts, shared/types/api.ts, shared/types/models.ts, shared/package.json, shared/tsconfig.json</files>
  <action>
1. Create `shared/` directory at project root

2. Create shared/package.json:
   - name: "@majalpost/shared"
   - version: "1.0.0"
   - main: "types/index.ts" (we'll use TypeScript path aliases, not compiled JS)
   - types: "types/index.ts"

3. Create shared/tsconfig.json:
   - Minimal config for type declarations
   - strict: true
   - declaration: true

4. Create shared/types/api.ts:
   - ApiResponse<T> type (success, data, error, message)
   - ApiError type (message, statusCode, details)
   - PaginatedResponse<T> type (data, total, page, limit, totalPages)

5. Create shared/types/models.ts:
   - Base entity type with _id, createdAt, updatedAt
   - Placeholder comment: "// Model types will be added in later phases"

6. Create shared/types/index.ts:
   - Re-export all types from api.ts and models.ts

Keep types minimal - only what's needed now. More types added as features are built.
  </action>
  <verify>TypeScript can resolve types: create a test.ts that imports from shared/types and run tsc --noEmit</verify>
  <done>Shared types package created with base API types</done>
</task>

<task type="auto">
  <name>Task 2: Configure TypeScript path aliases</name>
  <files>frontend/tsconfig.json, backend/tsconfig.json</files>
  <action>
1. Update frontend/tsconfig.json:
   - Add paths: { "@shared/*": ["../shared/*"] }
   - Add "../shared" to include array
   - Ensure baseUrl is set to "."

2. Update backend/tsconfig.json:
   - Add paths: { "@shared/*": ["../shared/*"] }
   - Add "../shared" to include array
   - Ensure baseUrl is set to "."

3. Update backend to use shared types:
   - Update src/types/index.ts to re-export from @shared/types
   - Or import directly where needed

4. Verify both projects can import from @shared/types without errors

Note: Next.js handles path aliases automatically. For backend, ts-node-dev needs tsconfig-paths or we use relative imports. Choose simplest approach: relative imports for backend if tsconfig-paths causes issues.
  </action>
  <verify>cd frontend && npm run build succeeds; cd backend && npm run build succeeds</verify>
  <done>Both frontend and backend can import shared types</done>
</task>

<task type="auto">
  <name>Task 3: Create API client for frontend</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/config.ts, frontend/.env.local, frontend/.env.example</files>
  <action>
1. Create frontend/.env.example:
   - NEXT_PUBLIC_API_URL=http://localhost:5000/api

2. Create frontend/.env.local (copy of .env.example)

3. Create frontend/src/lib/config.ts:
   - Export API_URL from process.env.NEXT_PUBLIC_API_URL
   - Add fallback to localhost:5000/api

4. Create frontend/src/lib/api.ts:
   - Create typed fetch wrapper function
   - Handle JSON parsing
   - Handle errors consistently
   - Return ApiResponse<T> type
   - Export convenience methods: api.get<T>(path), api.post<T>(path, data), etc.

5. Add a simple test in page.tsx (temporary):
   - Fetch from /api/health on component mount
   - Display connection status
   - Remove or comment out before phase complete (just for verification)

Use native fetch (available in Next.js), NOT axios. Keep it simple.
  </action>
  <verify>Run both frontend and backend, frontend page shows successful API connection</verify>
  <done>API client created, frontend can communicate with backend</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] `cd backend && npm run build` succeeds
- [ ] Shared types importable in both projects
- [ ] Frontend can fetch from backend health endpoint
- [ ] No TypeScript errors in any project
</verification>

<success_criteria>

- All tasks completed
- Shared types package created and usable
- Path aliases configured (or relative imports working)
- API client created with typed responses
- Frontend-backend communication verified
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
