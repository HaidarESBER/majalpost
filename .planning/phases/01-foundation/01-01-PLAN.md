---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/next.config.js, frontend/tailwind.config.js, frontend/src/app/layout.tsx, frontend/src/app/page.tsx, frontend/src/app/globals.css]
autonomous: true
---

<objective>
Initialize Next.js frontend with RTL support, Arabic typography, and Tailwind CSS.

Purpose: Create the foundation for the public-facing Arabic website with proper RTL layout and professional Arabic fonts (Almarai/IBM Plex Sans Arabic).
Output: Working Next.js app with RTL support, Arabic fonts loaded, and basic page structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with TypeScript and Tailwind</name>
  <files>frontend/package.json, frontend/next.config.js, frontend/tailwind.config.js, frontend/postcss.config.js, frontend/tsconfig.json</files>
  <action>
Create Next.js 14+ app in `frontend/` directory using App Router:
1. Run `npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` with default answers
2. After creation, verify the structure exists

Do NOT use Pages Router - use App Router only.
  </action>
  <verify>cd frontend && npm run build succeeds without errors</verify>
  <done>Next.js app created with TypeScript, Tailwind, ESLint, App Router, and src directory</done>
</task>

<task type="auto">
  <name>Task 2: Configure RTL support and Arabic fonts</name>
  <files>frontend/src/app/layout.tsx, frontend/src/app/globals.css, frontend/tailwind.config.js</files>
  <action>
1. Update layout.tsx:
   - Set `dir="rtl"` and `lang="ar"` on html element
   - Import Google Fonts: Almarai (primary) and IBM Plex Sans Arabic (secondary)
   - Configure font variables for Tailwind

2. Update globals.css:
   - Add font-family declarations using CSS variables
   - Set default text direction RTL
   - Add base Arabic typography styles (line-height: 1.8 for Arabic readability)

3. Update tailwind.config.js:
   - Extend fontFamily with 'almarai' and 'ibm-plex-arabic'
   - Add RTL-aware utilities if needed

Use next/font/google for font loading (automatic optimization).
  </action>
  <verify>cd frontend && npm run dev, then visually confirm RTL layout in browser at http://localhost:3000</verify>
  <done>RTL direction set, Arabic fonts loading, base typography configured</done>
</task>

<task type="auto">
  <name>Task 3: Create basic page structure with Arabic placeholder</name>
  <files>frontend/src/app/page.tsx, frontend/src/app/globals.css</files>
  <action>
1. Update page.tsx with simple Arabic content to verify RTL:
   - Heading: "مجال بوست" (Majal Post)
   - Subheading: "منصة إعلامية لبنانية مستقلة" (Independent Lebanese media platform)
   - Brief description paragraph in Arabic
   - Use Tailwind classes for styling

2. Apply category colors from PROJECT.md as CSS variables in globals.css:
   - --color-environment: emerald green
   - --color-society: violet
   - --color-economy: navy blue
   - --color-education: sky blue
   - --color-tech: teal
   - --color-health: coral/soft red

3. Set background to neutral white/light gray as specified in PROJECT.md

Keep it simple - this is just to verify the setup works.
  </action>
  <verify>cd frontend && npm run build && npm run dev shows Arabic content properly right-aligned with correct fonts</verify>
  <done>Homepage renders with Arabic content, RTL layout working, fonts displaying correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds without errors
- [ ] `cd frontend && npm run lint` passes
- [ ] Dev server shows RTL Arabic content at http://localhost:3000
- [ ] Fonts (Almarai) are loading (check Network tab or visual inspection)
</verification>

<success_criteria>

- All tasks completed
- Next.js 14+ with App Router running
- RTL layout working correctly
- Arabic fonts (Almarai, IBM Plex Sans Arabic) configured
- Category color variables defined
- No TypeScript or ESLint errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
