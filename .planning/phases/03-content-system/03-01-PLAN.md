---
phase: 03-content-system
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified: [shared/types/models.ts, backend/src/models/Category.ts, backend/src/models/Tag.ts, backend/src/routes/categories.ts, backend/src/routes/tags.ts, backend/src/routes/index.ts]
autonomous: true
---

<objective>
Create Category and Tag models with full CRUD APIs.

Purpose: Establish the foundational content taxonomy system that will organize articles. Categories represent the main navigation structure (البيئة والمناخ, شؤون الناس, etc.), while tags allow flexible cross-content tagging (#بيروت, #استدامة, etc.).

Output: Mongoose models for Category and Tag, RESTful API endpoints for both, shared TypeScript types, and integration into main router.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Category and Tag shared types</name>
  <files>shared/types/models.ts</files>
  <action>
1. Update shared/types/models.ts:
   - Add Category interface extending BaseEntity:
     * name: string (Arabic name, e.g., "البيئة والمناخ")
     * nameEn: string (English name for admin/internal use, e.g., "Environment & Climate")
     * slug: string (URL-friendly identifier, e.g., "environment-climate")
     * description?: string (optional Arabic description)
     * color: string (hex color code for theming, e.g., "#10b981" for emerald)
     * subCategories?: string[] (array of sub-category slugs, e.g., ["natural-wealth", "agriculture-land", "energy-sustainability"])
     * isActive: boolean (default: true, for soft enable/disable)
     * order: number (display order in navigation, default: 0)
   
   - Add Tag interface extending BaseEntity:
     * name: string (Arabic tag name, e.g., "#بيروت" or "استدامة")
     * slug: string (URL-friendly identifier, e.g., "beirut" or "sustainability")
     * isActive: boolean (default: true)

Keep types consistent with BaseEntity (_id, createdAt, updatedAt). Use descriptive names in Arabic.
  </action>
  <verify>shared/types/models.ts exports Category and Tag interfaces, TypeScript compiles</verify>
  <done>Category and Tag TypeScript types defined in shared types</done>
</task>

<task type="auto">
  <name>Task 2: Create Category Mongoose model</name>
  <files>backend/src/models/Category.ts</files>
  <action>
1. Create backend/src/models/ directory if it doesn't exist

2. Create backend/src/models/Category.ts:
   - Import mongoose, Schema from mongoose
   - Define CategorySchema with fields matching the Category interface:
     * name: { type: String, required: true, trim: true }
     * nameEn: { type: String, required: true, trim: true }
     * slug: { type: String, required: true, unique: true, lowercase: true, trim: true }
     * description: { type: String, trim: true }
     * color: { type: String, required: true, match: /^#[0-9A-Fa-f]{6}$/ }
     * subCategories: [{ type: String }]
     * isActive: { type: Boolean, default: true }
     * order: { type: Number, default: 0 }
     * timestamps: true (automatically adds createdAt, updatedAt)
   
   - Create and export Category model: mongoose.model('Category', CategorySchema)

Use Mongoose 9.x patterns. Ensure slug is unique and lowercase for URL safety.
  </action>
  <verify>Category model can be imported, mongoose.model('Category') works, schema validates required fields</verify>
  <done>Category Mongoose model created with validation</done>
</task>

<task type="auto">
  <name>Task 3: Create Tag Mongoose model</name>
  <files>backend/src/models/Tag.ts</files>
  <action>
1. Create backend/src/models/Tag.ts:
   - Import mongoose, Schema from mongoose
   - Define TagSchema with fields matching the Tag interface:
     * name: { type: String, required: true, trim: true, unique: true }
     * slug: { type: String, required: true, unique: true, lowercase: true, trim: true }
     * isActive: { type: Boolean, default: true }
     * timestamps: true (automatically adds createdAt, updatedAt)
   
   - Create and export Tag model: mongoose.model('Tag', TagSchema)

Use Mongoose 9.x patterns. Ensure both name and slug are unique.
  </action>
  <verify>Tag model can be imported, mongoose.model('Tag') works, schema validates required fields</verify>
  <done>Tag Mongoose model created with validation</done>
</task>

<task type="auto">
  <name>Task 4: Create Category CRUD API endpoints</name>
  <files>backend/src/routes/categories.ts</files>
  <action>
1. Create backend/src/routes/categories.ts:
   - Import Router, Request, Response from express
   - Import Category model
   - Import ApiResponse, ApiError, HttpStatus from backend types
   
   - Create router instance
   
   - GET /api/categories (list all categories):
     * Query params: ?isActive=true (optional filter)
     * Sort by order field, then by name
     * Return array of categories with ApiResponse wrapper
   
   - GET /api/categories/:slug (get single category):
     * Find by slug
     * Return 404 if not found
     * Return category with ApiResponse wrapper
   
   - POST /api/categories (create category):
     * Validate required fields (name, nameEn, slug, color)
     * Check for duplicate slug (return 409 Conflict if exists)
     * Create new category
     * Return 201 Created with category data
   
   - PUT /api/categories/:slug (update category):
     * Find by slug
     * Return 404 if not found
     * Update allowed fields (name, nameEn, description, color, subCategories, isActive, order)
     * Don't allow slug updates (must delete and recreate)
     * Return updated category
   
   - DELETE /api/categories/:slug (delete category):
     * Find by slug
     * Return 404 if not found
     * Delete category (hard delete for now)
     * Return 204 No Content
   
   - Export router as default

Handle errors with try/catch, use ApiError for operational errors, return ApiResponse format.
  </action>
  <verify>All category endpoints respond correctly, validation works, error handling returns proper status codes</verify>
  <done>Category CRUD API endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 5: Create Tag CRUD API endpoints</name>
  <files>backend/src/routes/tags.ts</files>
  <action>
1. Create backend/src/routes/tags.ts:
   - Import Router, Request, Response from express
   - Import Tag model
   - Import ApiResponse, ApiError, HttpStatus from backend types
   
   - Create router instance
   
   - GET /api/tags (list all tags):
     * Query params: ?isActive=true (optional filter), ?search=term (optional search by name)
     * Sort alphabetically by name
     * Return array of tags with ApiResponse wrapper
   
   - GET /api/tags/:slug (get single tag):
     * Find by slug
     * Return 404 if not found
     * Return tag with ApiResponse wrapper
   
   - POST /api/tags (create tag):
     * Validate required fields (name, slug)
     * Check for duplicate name or slug (return 409 Conflict if exists)
     * Create new tag
     * Return 201 Created with tag data
   
   - PUT /api/tags/:slug (update tag):
     * Find by slug
     * Return 404 if not found
     * Update allowed fields (name, isActive)
     * If name changes, generate new slug automatically
     * Return updated tag
   
   - DELETE /api/tags/:slug (delete tag):
     * Find by slug
     * Return 404 if not found
     * Delete tag (hard delete for now)
     * Return 204 No Content
   
   - Export router as default

Handle errors with try/catch, use ApiError for operational errors, return ApiResponse format.
  </action>
  <verify>All tag endpoints respond correctly, validation works, search functionality works, error handling returns proper status codes</verify>
  <done>Tag CRUD API endpoints implemented</done>
</task>

<task type="auto">
  <name>Task 6: Integrate routes into main router</name>
  <files>backend/src/routes/index.ts</files>
  <action>
1. Update backend/src/routes/index.ts:
   - Import categoryRoutes from './categories.js'
   - Import tagRoutes from './tags.js'
   
   - Add route mounting:
     * router.use('/categories', categoryRoutes)
     * router.use('/tags', tagRoutes)

Routes will be available at:
- /api/categories/*
- /api/tags/*
  </action>
  <verify>Routes are accessible at /api/categories and /api/tags, integration works with main router</verify>
  <done>Category and tag routes integrated into main API router</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run build` compiles without errors
- [ ] `cd backend && npm run dev` starts server successfully
- [ ] GET /api/categories returns list of categories (empty array initially)
- [ ] POST /api/categories creates a category successfully
- [ ] GET /api/categories/:slug returns single category
- [ ] PUT /api/categories/:slug updates category
- [ ] DELETE /api/categories/:slug deletes category
- [ ] GET /api/tags returns list of tags (empty array initially)
- [ ] POST /api/tags creates a tag successfully
- [ ] GET /api/tags/:slug returns single tag
- [ ] PUT /api/tags/:slug updates tag
- [ ] DELETE /api/tags/:slug deletes tag
- [ ] Validation errors return proper 400 status codes
- [ ] 404 errors return proper format
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- Category and Tag Mongoose models created with proper validation
- Full CRUD APIs for both categories and tags
- Shared TypeScript types exported
- Routes integrated into main router
- Error handling consistent with ApiResponse format
- No TypeScript errors
- APIs tested and working
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-system/03-01-SUMMARY.md`
</output>

