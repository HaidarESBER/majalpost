---
phase: 04-media-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/package.json, backend/src/models/Media.ts, backend/src/routes/media.ts, backend/src/routes/index.ts, backend/src/middleware/upload.ts, backend/src/utils/imageProcessor.ts, backend/src/config/env.ts]
autonomous: true
---

<objective>
Image upload API with local storage and thumbnail generation.

Purpose: Enable users to upload images that will be stored locally on the server, with automatic thumbnail generation for efficient loading and display.
Output: Working image upload endpoint with local file storage, thumbnail generation, and media metadata storage in MongoDB.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install required dependencies</name>
  <files>backend/package.json</files>
  <action>
1. Install multer for file upload handling:
   - npm install multer @types/multer

2. Install sharp for image processing and thumbnail generation:
   - npm install sharp

3. Install file-type for file validation:
   - npm install file-type@16

4. Verify package.json is updated with new dependencies

These packages provide:
- multer: Middleware for handling multipart/form-data (file uploads)
- sharp: High-performance image processing (resizing, thumbnails)
- file-type: Detect file types from buffers for validation
  </action>
  <verify>cd backend && npm install succeeds without errors</verify>
  <done>Dependencies installed: multer, sharp, file-type</done>
</task>

<task type="auto">
  <name>Task 2: Configure storage directories and environment variables</name>
  <files>backend/src/config/env.ts, backend/.env.example, backend/.env</files>
  <action>
1. Update backend/src/config/env.ts:
   - Add UPLOAD_DIR environment variable (default: './uploads')
   - Add THUMBNAIL_DIR environment variable (default: './uploads/thumbnails')
   - Add MAX_FILE_SIZE environment variable (default: '10MB' as string, parse to bytes)

2. Create storage directories structure:
   - Create uploads/ directory at backend root (gitignored)
   - Create uploads/original/ for full-size images
   - Create uploads/thumbnails/ for generated thumbnails

3. Update backend/.env.example:
   - Add UPLOAD_DIR=./uploads
   - Add THUMBNAIL_DIR=./uploads/thumbnails
   - Add MAX_FILE_SIZE=10485760 (10MB in bytes)

4. Update backend/.gitignore:
   - Add uploads/ directory to gitignore

Storage structure:
- backend/uploads/original/ - Full-size uploaded images
- backend/uploads/thumbnails/ - Generated thumbnails (e.g., 400x400px)
  </action>
  <verify>Environment variables accessible via env.UPLOAD_DIR, env.THUMBNAIL_DIR, env.MAX_FILE_SIZE</verify>
  <done>Storage directories created, environment variables configured</done>
</task>

<task type="auto">
  <name>Task 3: Create Media model for MongoDB</name>
  <files>backend/src/models/Media.ts, backend/src/models/index.ts</files>
  <action>
1. Create backend/src/models/Media.ts:
   - Define MediaSchema with mongoose:
     * filename: String (original filename)
     * originalPath: String (path to original file)
     * thumbnailPath: String (path to thumbnail, optional)
     * mimeType: String (image/jpeg, image/png, etc.)
     * size: Number (file size in bytes)
     * width: Number (image width in pixels, optional)
     * height: Number (image height in pixels, optional)
     * uploadedBy: ObjectId (reference to User, optional for now)
     * createdAt: Date (auto)
     * updatedAt: Date (auto)
   
   - Add indexes:
     * createdAt (for sorting by upload date)
     * mimeType (for filtering by type)

2. Export Media model and IMedia interface

3. Update backend/src/models/index.ts:
   - Export Media model

Keep it simple - basic metadata storage. More fields can be added later if needed.
  </action>
  <verify>Media model can be imported and instantiated without errors</verify>
  <done>Media model created with schema for image metadata</done>
</task>

<task type="auto">
  <name>Task 4: Create image processing utility</name>
  <files>backend/src/utils/imageProcessor.ts</files>
  <action>
1. Create backend/src/utils/imageProcessor.ts:
   - generateThumbnail function:
     * Accepts: image buffer, output path, options (width, height, quality)
     * Uses sharp to resize image
     * Default thumbnail size: 400x400px (maintaining aspect ratio)
     * Quality: 80% JPEG quality
     * Returns: Promise<void>
   
   - validateImage function:
     * Accepts: file buffer, mimeType
     * Validates file type (jpeg, png, webp, gif allowed)
     * Checks file size against MAX_FILE_SIZE
     * Returns: Promise<{ valid: boolean, error?: string }>
   
   - getImageDimensions function:
     * Accepts: image buffer
     * Uses sharp to get width and height
     * Returns: Promise<{ width: number, height: number }>

2. Handle errors gracefully (invalid format, processing failures)

3. Use sharp's metadata() for dimensions, resize() for thumbnails

Ensure sharp handles errors properly - wrap in try-catch blocks.
  </action>
  <verify>Test imageProcessor functions with sample image buffers in development</verify>
  <done>Image processing utility created with thumbnail generation, validation, and dimension extraction</done>
</task>

<task type="auto">
  <name>Task 5: Create multer upload middleware</name>
  <files>backend/src/middleware/upload.ts</files>
  <action>
1. Create backend/src/middleware/upload.ts:
   - Configure multer storage:
     * Use diskStorage (local filesystem)
     * Destination: UPLOAD_DIR/original/
     * Filename: generate unique filename (timestamp + random string + original extension)
     * Example: 1704067200000-abc123.jpg
   
   - Configure multer limits:
     * fileSize: MAX_FILE_SIZE (from env)
     * files: 1 (single file upload)
   
   - Create upload middleware:
     * Accept single file with field name 'image'
     * File filter: only images (jpeg, png, webp, gif)
     * Error handling for file size and type violations
   
   - Export: upload.single('image') middleware

2. Add file validation in fileFilter:
   - Check mimeType matches allowed types
   - Reject non-image files with clear error

Use multer's fileFilter option to validate file types before saving.
  </action>
  <verify>Middleware can be imported and used in route handlers, rejects invalid files</verify>
  <done>Multer upload middleware configured for single image uploads</done>
</task>

<task type="auto">
  <name>Task 6: Create media upload API routes</name>
  <files>backend/src/routes/media.ts, backend/src/routes/index.ts</files>
  <action>
1. Create backend/src/routes/media.ts:
   - POST /api/media/upload endpoint:
     * Use upload middleware from Task 5
     * Validate file exists
     * Generate thumbnail using imageProcessor
     * Get image dimensions
     * Save Media document to MongoDB:
       - filename, originalPath, thumbnailPath
       - mimeType, size, width, height
       - createdAt
     * Return ApiResponse with media document (without paths, return URLs)
     * Clean up on error (delete uploaded files if DB save fails)
   
   - GET /api/media/:id endpoint:
     * Fetch media by ID from MongoDB
     * Return media document with URLs
   
   - GET /api/media endpoint (list):
     * Optional query params: page, limit, mimeType
     * Return paginated list of media
     * Sort by createdAt descending (newest first)

2. File URL generation:
   - Original: /api/media/:id/file (will serve file in next task or separate plan)
   - Thumbnail: /api/media/:id/thumbnail (will serve file in next task or separate plan)
   - For now, return paths in response (URL generation can be refined later)

3. Error handling:
   - File upload errors (size, type)
   - Image processing errors
   - Database errors
   - Use ApiError class for consistent error responses

4. Update backend/src/routes/index.ts:
   - Import mediaRoutes
   - Add router.use('/media', mediaRoutes)

Keep response structure consistent with existing API patterns (ApiResponse<T>).
  </action>
  <verify>POST /api/media/upload accepts image file and returns media document, GET endpoints work</verify>
  <done>Media upload API routes created with upload, list, and get endpoints</done>
</task>

<task type="auto">
  <name>Task 7: Create file serving endpoints</name>
  <files>backend/src/routes/media.ts, backend/src/index.ts</files>
  <action>
1. Add file serving endpoints to backend/src/routes/media.ts:
   - GET /api/media/:id/file:
     * Fetch media document by ID
     * Check file exists on filesystem
     * Serve file with correct Content-Type header
     * Set appropriate caching headers (Cache-Control: public, max-age=31536000)
     * Handle 404 if file or document doesn't exist
   
   - GET /api/media/:id/thumbnail:
     * Fetch media document by ID
     * Check thumbnail exists on filesystem
     * Serve thumbnail with correct Content-Type header
     * Set caching headers
     * Fallback to original file if thumbnail doesn't exist (graceful degradation)
     * Handle 404 if file doesn't exist

2. Use express's sendFile() method:
   - Set root option for security
   - Set appropriate headers (Content-Type, Cache-Control)
   - Handle errors (file not found, permission errors)

3. Ensure path security:
   - Validate file paths (prevent directory traversal)
   - Use path.join() or resolve paths safely
   - Never serve files outside UPLOAD_DIR

Keep serving logic simple - direct file serving from filesystem. CDN or advanced serving can come later.
  </action>
  <verify>GET /api/media/:id/file serves original image, GET /api/media/:id/thumbnail serves thumbnail</verify>
  <done>File serving endpoints created for original images and thumbnails</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && npm run build` succeeds without errors
- [ ] `cd backend && npm run lint` passes
- [ ] POST /api/media/upload accepts image file and creates media document
- [ ] Thumbnails are generated successfully (check uploads/thumbnails directory)
- [ ] GET /api/media/:id/file serves original image
- [ ] GET /api/media/:id/thumbnail serves thumbnail
- [ ] GET /api/media returns paginated list of media
- [ ] File size and type validation works (rejects large files, non-images)
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- Image upload API endpoint functional
- Files stored locally in uploads/original/
- Thumbnails generated and stored in uploads/thumbnails/
- Media metadata stored in MongoDB
- File serving endpoints work for both original and thumbnails
- File validation (size, type) implemented
- Error handling for all failure cases
- No TypeScript or linting errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-media-management/04-01-SUMMARY.md`
</output>

